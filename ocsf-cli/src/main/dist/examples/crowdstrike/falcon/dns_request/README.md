# DNS Request event translation to OCSF DNS Activity event

To translate the example event, use the following command:
```bash
bin/ocsf-cli -R examples -r dns_request examples/crowdstrike/falcon/dns_request/raw.json
```
